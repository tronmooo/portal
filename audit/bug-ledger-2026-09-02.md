# Bug ledger — continuous error hunting, 2026-09-02

Method: drive the DEPLOYED app (portol.me) through the smoke account with
scripted user flows (create → view everywhere → edit → verify → delete → verify),
plus AI-chat natural-language sessions, and static audits of shared/server/client.
Every FIXED entry was reproduced before the change and re-verified against
production after deploy. Unit coverage: `tests/audit-fixes-2026-09.test.ts`.

Status key: FIXED · OPEN · NEEDS INVESTIGATION · NOT A BUG

## Found by driving the app

| # | Issue | Repro | Expected | Actual | Root cause | Scope | Fix | Verification | Status |
|---|---|---|---|---|---|---|---|---|---|
| D1 | Dashboard overdue-task widget lists a task due today | POST task dueDate=today (LA evening = next UTC day); GET /dashboard-enhanced | Not in overdueTasks | Listed as overdue | `new Date("YYYY-MM-DD") < now` (UTC midnight vs instant) in getDashboardEnhanced | Same class fixed earlier in stats/insights/notifications | Compare calendar days in user tz via shared `localDayOf` | Live: due-today excluded, yesterday included | FIXED |
| D2 | "Today's events" ignores recurrence | POST daily event dated 2024-01-08; GET /dashboard-enhanced | Occurs today | Empty | `allEvents.filter(e => e.date === today)` | Any surface asking "what happens today" | New shared `eventOccursOn()` (recurrence, skips, pause, end) | Live: daily-from-2024 and weekly-7-days-ago appear; weekly-6-days-ago does not | FIXED |
| D3 | Marking a bill paid logs the expense on the DUE date | POST /obligations (due +5d) → POST /pay {} | Expense dated today | Expense dated +5d (future spend) | `paymentDate = input.paymentDate \|\| occurrenceDate`; UI sends no date | Every "Mark paid" surface, monthly spend, budgets | Default to today when the occurrence is still upcoming; keep due date for catch-up on past occurrences | Live: future bill → today; overdue bill → due date | FIXED |
| D4 | KPI monthly-obligations total counts paused bills, popup doesn't | Create bill, pause it; compare /stats vs /dashboard-enhanced | Equal | Differ by paused amount | getStats reduce had no status rule | ARCHITECTURE §10.1 | Shared `isActiveObligation()` used by both | Live: totals equal; paused excluded | FIXED |
| D5 | Tracker accepts `{weight:"abc"}` | POST /trackers/:id/entries on a tracker whose declared field is `value` | 400 | 201 with `validated:false` | Guard coerced only declared numeric fields | All 17 entry write paths | Numeric-by-name keys validated regardless of declaration | Live: 400 | FIXED |
| D6 | Deleted habit's mirror tracker stays on Trackers page | Create habit, check in, delete habit; GET /trackers | Tracker gone (recoverable) | Tracker + check-in entries still listed | deleteHabit soft-deletes the habit only | Trackers page, stats.totalTrackers, health snapshot | Retire the mirror tracker (only when it holds nothing but this habit's `_habitId` entries) with the habit; revive on restore | Live: hidden after delete, back with entry after restore; user's own linked tracker survives | FIXED |
| D7 | Hydration "today" tile hardcoded Pacific | code read | User tz | LA | literal timeZone | getDashboardEnhanced | `getUserToday(this._timezone)` | unit suite | FIXED |
| D8 | AI "I ran 2 miles" → reply says logged, immediate GET /trackers shows no running tracker | Chat then GET | Visible | Not visible once; visible on retry minutes later | Not reproducible with a fresh tracker type (yoga: visible immediately). Another session was concurrently testing the same lane on the same account | — | — | Re-run passed | NEEDS INVESTIGATION (likely contention) |
| D9 | Expiring-document notification missing at +10 days | POST document with extractedData.expirationDate=+10d; GET /notifications | Expiry alert? | None; dashboard lists it (30-day window) | Bell uses a 7-day window by design; dashboard uses 30 | Two thresholds, both intentional | — | Re-tested at +5d: alert present | NOT A BUG |
| D10 | Editing a bill's due date leaves the calendar on the old dates | POST bill due +3d; PATCH nextDueDate=+12d; GET /calendar/timeline | Occurrence on +12 | Still +3 and +33; bill, popup and date-rules show +12 | Schedule anchors on `firstPaymentDate`, set at create and never updated by the edit | Calendar, upcoming feed, bill schedule section | Re-anchor on explicit edit; schedule walks the series from the anchor and emits pre-anchor paid/skipped overrides as standalone history (walking from an off-grid override dropped the new day-of-month) | Unit: off-grid history + new grid; live re-verify pending deploy | FIXED |
| D6b | Regression of the D6 fix: a user's OWN tracker linked to a habit was retired with the habit | Create tracker, log entry, link habit, check in, delete habit | Tracker survives | 404 | Ownership lookup selected a non-existent column (`values` vs `entry_values`); the error made `every()` vacuously true | Any habit with a linked tracker | Fail closed on lookup error; require ≥1 mirror entry or the auto-created name for an empty tracker | Supabase query: only smoke-account probe rows affected; live re-verify pending | FIXED |
| D11 | "I weigh 80 kg" stored as 80 in a pounds tracker | Chat "I weigh 80 kg today" on the lbs Weight tracker | 176.4 stored (tracker unit) | `{value:80, unit:"kg"}` stored; chart drops 100 lb | normalizeTrackerEntry only read a unit written INSIDE the value ("80 kg"); the AI lane emits a sibling `unit` key | Every quick-log lane, smart-entry, extraction | Normalizer resolves value suffix → `<key>Unit` → bare `unit` sibling (single-measurement entries only), converts, drops the spent key | Unit tests; live re-verify pending deploy | FIXED |
| D12 | Browser-level UI crawl (console errors, failed requests, renders) | Playwright against portol.me | — | Chromium gets ERR_CONNECTION_RESET with and without the egress proxy | Sandbox blocks browser egress | UI-only defects remain untested by execution | — | — | BLOCKED (environment) |
| D13 | Chat "add a Netflix subscription" appears twice (obligations + profiles) | — | — | — | Obligations are a projection of recurring-bill liability profiles; one record, two views | — | — | Price change updated in place | NOT A BUG |
| D14 | Chat "log 8 hours" stores sleep quality "excellent" the user never said | — | — | — | Documented derivation (≥8h → excellent) in the sleep lane prompt | — | — | — | NOT A BUG (design) |
| D15 | Bank CSV import rejects every debit and imports the paycheck as an expense | POST /import/bank-csv with a typical export (spending negative, deposit positive) | 3 expenses, deposit ignored | 0 expenses + $2,500 "expense"; errors "amount must be a positive number" | Amount handed straight to the expense validator; no sign convention | Every bank export with negative debits; monthly spend, budgets | Pure parser (server/bank-csv.ts) detects the file's convention (negative debits / Debit+Credit columns / positives), skips credits, keeps quoted commas, never re-parses ISO dates through UTC | Unit tests; live re-verify pending deploy | FIXED |
| D16 | Re-importing the same statement duplicates every row | Import the same CSV twice | Second import: 0 created | Rows doubled | No dedupe against existing expenses | Monthly spend, budgets, category totals | Skip rows matching an existing expense on day + cents + text (multiset, so two identical legit rows still import) | Unit test for the key; live re-verify pending | FIXED |
| D17 | POST /trackers/:id/entries stores `{ value: 70, unit: "kg" }` verbatim in a lbs tracker | Form/API write with a sibling unit | 154.3 stored | 70 + junk key | The unit normalizer ran only in the AI lanes, not at the write gate | Every non-AI write path (form, habit mirror, extraction) | normalizeTrackerEntry now runs inside logEntry (both storages) before the value guard | Unit tests; live re-verify pending | FIXED |
| D18 | Two or three simultaneous "Mark paid" taps record two payments and two expenses | Promise.all of 2–3× POST /obligations/:id/pay | One payment, one expense, due date advanced once | Two ledger rows and two expenses; in most rounds the second request had read the bill AFTER the first advanced the due date and paid the NEXT occurrence (Sept and Oct) | Read-then-write with no cross-instance guard; the route's 8-second in-memory dedupe does not span Vercel instances | Bills, monthly spend, budgets, account balances | (1) Occurrence stamp written FIRST as a compare-and-set on the row's updated_at (`claimBillOccurrence`), loser answers with the winner's payment; (2) an implicit "pay what's due" arriving within 8 s of a just-posted stamp on the same bill is folded into that payment (explicit occurrence dates still pay); ledger row carries the pre-stamped id; winner lookup retries briefly; route propagates `deduped` | Unit tests (winner / loser / implicit re-pay / explicit occurrence); DB inspection of failing rounds; live re-verify pending deploy | FIXED |
| D19 | Three simultaneous check-ins create two check-ins on a daily habit | Promise.all of 3× POST /habits/:id/checkin | One check-in | Two rows (streak and mirror stayed at 1) | Read-then-insert race in checkinHabit | Streaks, completion rate, habit history | Post-insert reconciliation at the write gate: surplus rows beyond targetPerDay (oldest win) are deleted by whichever request runs; a loser is handed the surviving row | Live re-verify pending deploy | FIXED |
| D20 | A revoked artifact share link still serves content | Revoke, then GET /api/public/artifacts/:token | 404 | 200 for ≤60 s, then 404; a cache-busting query 404s immediately | Public route sets `Cache-Control: public, max-age=60`, deliberately (enumeration throttling) | — | — | Verified 404 after 65 s | NOT A BUG (60 s edge cache by design) |
| D21 | Double-submitting an identical expense creates two rows | 2× POST /expenses concurrently | — | Two rows | No idempotency key; clients guard with disabled buttons | — | — | — | NOT A BUG (accepted design, noted) |
| D23 | Receipt review pre-selects the line items AND the total as expenses | Upload a receipt whose extracted line items carry no date | One expense (the total) | Three pre-selected expenses ($8.99 + $5.49 + $14.48) when the extractor leaves line items undated | componentAmountIds grouped money facts by subject+date, so undated components and the dated total never met | Every receipt/invoice review | Undated facts join every dated group of their subject | Unit tests; production plans now select only the total | FIXED |
| D24 | "Start tracking Last4 — $1111" proposed from a card's last four digits | Upload a receipt paid by card | No tracker proposal | Unselected tracker proposal with value 1111 USD | Identifier regex had no last-4 / "ending in" pattern; four digits do not look like an id by shape | Any document with a masked card/account | IDENT covers last 4 / last four / ending in / card ending | Unit test | FIXED |
| D25 | Confirming a review returned 404 "Resource not found" ~10 min after upload | Save a plan, wait, confirm | Success | 404 | The contract suite's fixture reset (a concurrent push) deleted the document in between; immediate confirm works on every run | — | — | Upload→confirm twice: one expense, provenance link written, double-confirm idempotent | NOT A BUG (test-environment artifact) |
| D26 | Paying a bill "from an account" through the bills route never moves the account balance | POST /obligations/:id/pay {accountId} | Balance −amount | Unchanged (the occurrence route honoured accountId; this one dropped it) | Route destructured only amount/method/date | API parity; no UI sends accountId today | accountId validated and passed through | Live re-verify pending | FIXED |
| D27 | Adding an owner who is already linked returns 500 "Internal server error" | POST /liability-profile-links (or asset-party-links) twice for the same party | 409 with guidance | 500 | Postgres unique violation thrown through the generic handler | Ownership editor | Unique violation → 409 "already an owner; edit the existing share" | Live re-verify pending | FIXED |
| D28 | A co-signer with a share appears on no dashboard | Link role co_signer 50% | — | Share counted for owner-type roles only | resolveOwners counts ownership roles; a co-signer is liable, not an owner | — | — | Owner-role 50/50 shows 4000/4000; overflow past 100% rejected | NOT A BUG (design) |
| D29 | Same-instance duplicate tap answered `{deduped:true}` with no payment row | 3× POST /pay while the first is in flight | Same row for every tap | Empty ack for the tap that arrived mid-flight | Route window stored `payment: null` until the first finished | Bill UIs rendering the returned payment | Route stores the in-flight promise and awaits it for duplicates | Live re-verify pending | FIXED |
| D30 | Chat says an OVERDUE internet bill is "paid off" | Ask "which bills are overdue or due this week?" with the fixture's overdue utility bill | Bill listed as overdue | "shows a $0 balance (paid off), so it's not generating a due date"; also invented "due today" for a mortgage with no due date | System-prompt liability line labelled bal 0 as PAID-OFF (a recurring bill has no balance by design) and never stated the next due date | Every bill question in chat | Recurring bills get status from their due date (overdue/upcoming/paid) plus `amount: $X/month, due: <date>`; loans without a due date are told "NONE RECORDED — never present a due date" | Live: internet bill now reported overdue; the model still guessed "due today" for the mortgage once before the stronger wording — re-verify pending | FIXED (bill) / loan wording root-caused as D33 |
| D31 | "undo that" after "I paid X" claims success but only rolls the due date back | Chat pay, then "undo that"; inspect ledger, occurrence stamp, expense | Payment, paid stamp, expense and account debit all retracted | Ledger row, paid stamp, lastPaidDate and expense all remain; due date rolled back | The action log's generic `reapply_before` plan restored the obligation's nextDueDate only; the real inverse (unpayBillOccurrence) was never used | Every chat-driven bill/loan payment undo | pay_obligation / add_liability_payment record an `unpay_bill` plan with the payment id; the reverse executor calls unpayBillOccurrence | Unit test; live re-verify pending deploy | FIXED |
| D32 | Chat "I spent $7" while the dashboard filter is on Smoke Spouse creates the expense for Self | POST /chat with x-active-profile-ids=spouse | (probe assumed) spouse | Self | The chat client deliberately sends no profile filter: attribution comes from the words ("I" = the user), while a form opened on a person's page auto-owns to that person (both documented) | — | — | Named person ("Smoke Child spent $4") attributes correctly | NOT A BUG (design) |
| D33 | A loan with no due date is "due today" every day — on the calendar, the schedule, and in chat | Fixture mortgage (balance + monthly payment, no due date/due day). GET /calendar/timeline for today, GET /liabilities/:id/schedule, ask chat "which bills are due this week?" | No occurrence and no next due until the user sets a due day/date; chat never states a date | Timeline: `2026-09-02 pending payment $2200` (was `2026-09-01` the day before — the date moves with the clock); schedule.nextDue = today, status `due_today`; chat: "Smoke Mortgage — $2,200 due today" | `deriveScheduleFields` fell back to `due = todayISO` when a loan had neither a due date nor a due day, so the series anchored on today and re-anchored daily; the prompt-only fix (D30) could not stop the model repeating the calendar tool's answer | Every loan/credit card/one-time debt without a due date: calendar, liability schedule, AI chat | CODE FIX in shared/liability-schedule.ts: anchor order is explicit date → due day → last recorded payment + one cycle → NONE (no dueDate/nextDueDate/firstPaymentDate emitted, so `seriesAnchor` is null and no occurrences are generated; `nextDue` is null and the detail page already renders that state) | Unit: three new tests; live: re-probe timeline + schedule + chat after deploy (see below) | FIXED — live verification below |
| D34 | Chat reports a dismissed notification as "currently active" | Create an overdue task → it appears in /api/notifications as `task-overdue-<id>`; dismiss it (write the id to the `dismissed_notifications` preference, as the bell does); ask chat "do I have any notifications right now?" | The dismissed alert is not listed | Chat: "1. 🔴 Overdue Task — Notif probe … These are system-generated alerts … You don't have any custom/manual notifications in your bell" | Dismissal was applied only client-side (bell and dashboard briefing each re-read the preference); `buildNotifications` served the unfiltered list to every other reader and the chat had no bell data at all, so it improvised from the overdue items | Chat answers about notifications; any future server-side reader (digests, cron) | CODE FIX: `buildNotifications` drops dismissed ids at the source (`readDismissedNotificationIds`), and the chat prompt gets a "Bell notifications (N)" line built from the same function | Unit: 2 new tests in notifications-v2; live: re-probe after deploy (below) | FIXED — live verification pending |
| D35 | Loan terms editor cannot clear a due day, first payment date or lender | Liability detail → Edit terms → blank the Due day field → Save | Due day removed; the loan leaves the calendar until a date is set | "Loan terms saved" toast, but the old due day is still stored and the payment stays on the calendar | `saveTermsMutation` skipped every blank input (`if (tDueDay !== "")`), while the server treats `null` as delete (the bill editor on the same page already relies on that) | Every loan / credit card / one-time debt terms edit | CODE FIX in liability-detail.tsx: due day, first payment date and lender send `null` when blanked and previously stored; money terms keep skip-on-blank so a $0 balance (rendered blank) cannot be cleared into a fallback to the original balance | Typecheck; API semantics verified live (PATCH `{fields:{dueDay:null}}` removes the key); UI path not browser-tested (D12 blocker) | FIXED (code) |

## Verified working (no bug)
Expense lifecycle across list/stats/enhanced/bootstrap/profile-detail/search/scoped filters; profile isolation for every entity type; orphan-item Self rule; profile deletion cascade (expense, task, habit, event, obligation, timeline, notifications, search); habit check-in/undo/streak/completion rate; same-day duplicate check-in guard; old daily series on calendar timeline; goal progress and auto-complete; journal streak; budgets upsert; biweekly income occurrences; document expiration on timeline/date-rules/profile detail and cleanup on delete; receipt PDF upload → extraction (vendor, date, items, total, last4) → review → confirm creates one expense with a document provenance link, idempotent on re-confirm; profile birthdays recur yearly and move with a DOB edit; licence expiry from profile fields reaches the dashboard and calendar; accounts: bill paid from an account (occurrence route) debits it and unpay restores it, rolls the due date back and retracts the expense; loans: payment splits principal/interest, reduces the balance on the dashboard, logs no expense; 50/50 owner shares show half on each person's dashboard and the whole once for everyone; ownership overflow past 100% rejected; chat read answers (month spend, net worth, food total) match the API to the cent; chat refuses to delete a person or all expenses without confirmation; chat "I paid X" records one payment; AI chat: relative dates in user tz, in-place correction, other person's expense, three-action message, tracker log with units, no money-shaped trackers.

## Found by static audit (rounds 1–2, see commits 8035da1, e7b5f09, 003aa6a6)
49 items across server/shared/client — reset-token takeover, PostgREST filter injection, CSRF exemption path, email HTML injection, due-today-is-overdue, tile≠popup, Stripe webhook after ack, unscheduled crons, phantom cache keys, UTC "today", month bucketing, recurrence expansion cap, amortization overflow, loan double count, frequency vocabulary drift, cross-tenant artifact upsert, finance-sync refresh id, etc. All FIXED with tests.
