# Portol AI — capabilities manifest

> Auto-generated by `scripts/gen-ai-capabilities.ts` from `server/ai-engine.ts`.
> Do not edit by hand. Regenerate after changing `TOOL_DEFINITIONS`.

**134 tools** — 99 write, 35 read-only.
The AI chat dispatches these via Anthropic tool-calling (`processMessage`), and
`tests/ai-tool-registry.test.ts` guards that every tool stays wired.

## Write tools (99)

| Tool | Action type | Description |
|---|---|---|
| `add_liability_payment` | `add_liability_payment` | Record a payment (or non-payment) against a liability. Use for: 'paid $500 on my car loan', 'made the mortgage payment', 'sent $2000 extra p |
| `bulk_complete_tasks` | `complete_task` | Mark multiple tasks as complete at once. Use when user says 'complete all tasks', 'mark everything done', or 'finish all overdue tasks'. |
| `checkin_habit` | `checkin_habit` | Check in to a habit — mark it DONE for today. Use this whenever the user says 'I did X', 'mark X done', 'completed X habit', 'checked off X' |
| `complete_event` | `complete_event` | Mark a calendar event as completed/attended. Use when user says 'I went to X', 'I attended X', 'X is done', 'mark X event complete', 'I comp |
| `complete_task` | `complete_task` | Mark a task as DONE/COMPLETE. Use this when user says 'I completed X', 'mark X as done', 'finished X task', 'checked off X', 'did X', 'I did |
| `configure_dashboard_sections` | `update_entity` | Show, hide, or reorder DASHBOARD SECTIONS — 'hide the Quick Notes section', 'show the Finance section on my dashboard', 'move Goals to the t |
| `confirm_paycheck_received` | `log_paycheck` | Confirm a paycheck was received. Marks it as confirmed with the actual received date and amount. |
| `convert_expense_to_asset` | `revalue_asset` | Convert a one-time expense into a tracked asset profile. Use when the user says something like 'that laptop I expensed is actually an asset' |
| `copy_budgets_previous_month` | `set_budget` | Copy ALL of the previous month's category budgets into a target month (defaults to the current month). Use for 'copy last month's budgets',  |
| `create_artifact` | `create_artifact` | Create a rich artifact — markdown doc, code snippet, chart, diagram, checklist, or note. Use this when the user asks for reports, analysis,  |
| `create_budget` | `set_budget` | Create a new monthly budget for a spending category. Use when the user wants to add a budget they don't already have. For updating an existi |
| `create_document` | `manage_document` | Create a new stored document/record (passport, license, insurance card, warranty, note-as-document, etc.). Use this for 'create a document', |
| `create_domain` | `manage_domain` | Create a new custom domain/category for tracking custom data. |
| `create_event` | `create_event` | Create a calendar event. For asset/appliance maintenance reminders (e.g. 'replace fridge filter every 6 months', 'HVAC service yearly'), use |
| `create_expense` | `log_expense` | Log a one-time financial expense. Use this when the user says 'spent X on Y', 'paid X for Y', 'bought X', or mentions any one-time payment.  |
| `create_goal` | `create_goal` | Create a new measurable goal. Use when user says things like 'I want to lose 10 lbs by June' or 'My goal is to run 100 miles this quarter' o |
| `create_habit` | `create_habit` | Create a new habit to track. Set timeOfDay when the user says when it should happen (e.g. 'take lisinopril in the morning' → morning, 'medit |
| `create_liability` | `create_liability` | Create a first-class LIABILITY profile (a debt the user owes). Use for credit cards, mortgages, auto loans, student loans, personal loans, H |
| `create_notification` | `update_entity` | Create a PERSISTED notification in the notification bell — 'notify me about the insurance renewal', 'create an urgent notification that rent |
| `create_obligation` | `create_obligation` | Create a RECURRING bill, subscription, loan payment, or ongoing financial obligation that repeats on a schedule. ONLY use this when the user |
| `create_profile` | `create_profile` | Create a new profile. Choose the right type and include entity-specific fields. Pet: breed, species, color, birthday, weight. Vehicle: make, |
| `create_reminder` | `create_reminder` | Create a REAL reminder that fires a notification at a specific date AND time AND places it on the calendar as an event (it shows up on the C |
| `create_task` | `create_task` | Create a new TASK — an undated or date-only to-do with NO clock time ('add a task to buy milk', 'remind me to renew registration before Aug  |
| `create_tracker` | `create_tracker` | Create a smart tracker that auto-generates the right fields for ANY domain. YOU decide the fields based on what the user wants to track. FIE |
| `delete_artifact` | `delete_entity` | Delete an artifact (note, checklist, markdown, code, chart, diagram, etc.) by title. |
| `delete_budget` | `set_budget` | Delete a budget for a specific category and month. |
| `delete_domain` | `manage_domain` | Delete a domain by name. |
| `delete_event` | `delete_entity` | Delete a calendar event by title. |
| `delete_expense` | `delete_entity` | Delete an expense by description. |
| `delete_goal` | `delete_entity` | Delete a goal by title. |
| `delete_habit` | `delete_habit` | Delete a habit by name. This also removes all check-in history. |
| `delete_income` | `delete_entity` | Delete an income entry by its description/source (partial match). Use for 'delete the freelance income', 'remove that $500 income I logged'. |
| `delete_journal` | `delete_entity` | Delete a journal entry by date. |
| `delete_liability_payment` | `delete_entity` | Delete a RECORDED payment from a liability/loan/bill's payment history ('remove that duplicate mortgage payment', 'delete the payment I logg |
| `delete_memory` | `delete_entity` | Delete a saved memory/fact by key or content match. |
| `delete_obligation` | `delete_entity` | Delete a recurring obligation/bill by name. |
| `delete_paycheck` | `delete_entity` | Delete an expected paycheck entry. Find it by source (partial match) and optionally its expected date. Use for 'delete the Acme paycheck', ' |
| `delete_profile` | `delete_entity` | Delete a profile by name. |
| `delete_reminder` | `delete_entity` | Delete a pending reminder ('cancel my dentist reminder', 'delete the medication reminders'). Finds ALL un-fired occurrences matching the tit |
| `delete_task` | `delete_task` | Permanently delete a task by title. Use when user says 'delete X task', 'remove X', 'get rid of X task'. NEVER use this when user says 'comp |
| `delete_tracker` | `delete_entity` | Delete a tracker and ALL its entries. This is irreversible. |
| `delete_tracker_entry` | `delete_tracker_entry` | Delete a specific logged entry from a tracker. Use when user says 'remove my X entry', 'delete that X log', 'undo my X log from today'. Remo |
| `dismiss_notifications` | `update_entity` | Dismiss notification(s) from the notification bell — 'dismiss all my notifications', 'clear the bill notifications', 'dismiss the passport e |
| `duplicate_artifact` | `create_artifact` | Duplicate an existing artifact ('make a copy of my packing list'). The copy gets ' (copy)' appended (or newTitle), is never pinned, and is n |
| `execute_bulk_action` | `delete_entity` | PHASE 2 of a bulk delete — run ONLY after preview_bulk_action AND the user explicitly confirmed the preview in a later message. Re-derives t |
| `journal_entry` | `journal_entry` | Create a journal entry OR a QUICK NOTE for the user or a specific profile. This is the ONLY tool for notes — the dashboard's 'Quick Notes' s |
| `link_asset_owner` | `link_entities` | Assign an owner / co-owner / beneficiary / trustee to an asset with an ownership percentage. Use for 'split the Porsche 60/40 with Sarah', ' |
| `link_asset_to_liability` | `link_entities` | Attach an asset to a liability (collateral, secured_by, etc). Use for 'this loan covers my Tesla', 'mortgage on 123 Maple', 'add appliance f |
| `link_entities` | `link_entities` | Create a link between two entities. Use when the user says 'link X to Y', 'X belongs to Y', or when you detect a relationship between entiti |
| `link_liability_asset` | `link_entities` | Link a liability to a collateral asset. Use for: 'this mortgage is on my house', 'the auto loan is for my Civic', 'HELOC against the propert |
| `link_liability_owner` | `link_entities` | Assign an owner / co-signer / guarantor to a liability with an ownership percentage. Use for: 'this loan is split 50/50 with my wife', 'my d |
| `log_expected_paycheck` | `log_paycheck` | Log an expected paycheck with source, amount, and expected date |
| `log_income` | `log_income` | Log an income entry. Use when user says 'I got paid $X', 'received $X from freelance', 'paycheck of $X', or mentions any incoming money. |
| `log_medication_dose` | `log_entry` | Log a TAKEN medication dose — 'I took my Lisinopril', 'gave Max his Heartgard this morning', 'took my meds'. Resolves the medication tracker |
| `log_tracker_entry` | `log_entry` | Log values to a tracker (health, fitness, habits, metrics — NEVER money: anything spent/paid/bought such as repairs, maintenance costs, purc |
| `manage_document` | `manage_document` | Rename, delete, re-extract, or RE-LINK a stored document to a person/profile. Actions: 'rename', 'delete', 're_extract' (re-reads the saved  |
| `mark_loan_payment` | `pay_obligation` | Mark a scheduled amortization payment as PAID on a loan's payment schedule (the Finance → Loans table). Targets the next unpaid installment  |
| `mark_notifications_read` | `update_entity` | Mark notifications as READ — 'mark all my notifications as read', 'mark the insurance one read'. Only user-created (custom) notifications ca |
| `merge_profiles` | `update_profile` | MERGE one profile into another — 'merge the duplicate Mike profiles', 'combine Mom and Mother into one'. PHASE 1 only: returns a PREVIEW (re |
| `move_liability_to_asset` | `update_entity` | Move a liability's collateral attachment from one asset to another atomically. Use for 'move the appliance financing from the old house to t |
| `pay_obligation` | `pay_obligation` | Record a payment for an obligation. Find by name. By default pays the oldest open occurrence. To pay a SPECIFIC month, pass forMonth (YYYY-M |
| `preview_bulk_action` | `update_entity` | PHASE 1 of a bulk delete — 'delete all my test tasks', 'clear out everything for the Demo profile', 'remove all expenses containing lunch fr |
| `recall_memory` | `recall_memory` | Search EVERYTHING the user has ever told the app: saved memories, every profile and every profile field (pet breeds, vehicle VIN/mileage/yea |
| `refund_expense` | `log_expense` | Record a refund against a previously logged expense. Use when the user says 'I got refunded $40 for the shoes', 'return the headphones I bou |
| `repair_relations` | `update_entity` | FIX the ownership issues found by find_orphans — 'repair my data', 'fix the broken links', 'clean up the orphans'. Run find_orphans first an |
| `restore_habit` | `update_entity` | Restore (undelete) a recently DELETED habit — 'bring back my meditation habit', 'restore the habit I deleted'. Searches soft-deleted habits  |
| `restore_task` | `update_entity` | Restore (undelete) a recently DELETED task — 'bring back the task I just deleted', 'restore the passport task'. Searches soft-deleted tasks  |
| `revalue_asset` | `revalue_asset` | Re-estimate the current market value of a vehicle, asset, or property profile using LIVE web search data from Zillow, KBB, Edmunds, etc. Use |
| `save_memory` | `save_memory` | Save an ABSTRACT preference or piece of context that does NOT belong to a specific profile — e.g. 'I prefer window seats', 'I'm vegetarian', |
| `schedule_medication_refills` | `create_event` | Create REAL calendar reminders for a prescription's refills. Call this WHENEVER the user describes a prescription with a quantity and refill |
| `set_budget` | `set_budget` | Set or update a monthly budget for a spending category. Creates or updates the budget amount for a specific category and month. |
| `set_notification_preferences` | `update_entity` | Mute or unmute notification categories — 'mute info notifications', 'stop showing bill alerts', 'unmute everything'. Muted severities/types  |
| `skip_medication_dose` | `log_entry` | Log a SKIPPED or MISSED medication dose — 'I skipped tonight's dose', 'forgot my Metformin yesterday', 'didn't give Max his pill, he was sic |
| `split_ownership` | `link_entities` | Atomically replace all ownership on an asset OR liability with a new split across multiple parties. Use for 'split the Tesla 60/40 between m |
| `sync_calendar` | `update_entity` | Sync events with Google Calendar. Imports new events from Google Calendar into Portol. Use when the user asks to sync, import, or pull their |
| `toggle_artifact_item` | `update_entity` | Check or uncheck an item on a checklist artifact — 'check off milk on my grocery list', 'uncheck the passport item on the packing list'. Fin |
| `uncomplete_habit` | `uncomplete_habit` | Remove/undo a habit check-in for today (or a specific date). Use when user says 'unmark X habit', 'undo my X checkin', 'I didn't actually do |
| `undo_last_action` | `update_entity` | UNDO a previous chat action — 'undo that', 'undo my last action', 'take that back', 'I didn't mean to do that'. Reverses the most recent rev |
| `undo_last_payment` | `delete_entity` | Undo the MOST RECENT payment recorded on a recurring bill/obligation — removes the payment row so the bill shows unpaid again. Use for 'undo |
| `unlink_asset_from_liability` | `link_entities` | Detach an asset from a liability. Use for 'remove the Tesla from this loan', 'this loan no longer covers the fridge'. |
| `update_artifact` | `update_entity` | Update an existing artifact's title, content, or pinned state. Use for edits AND for 'pin my grocery list' (changes.pinned:true) / 'unpin it |
| `update_budget` | `set_budget` | Update an existing budget's amount or category. Use when the user wants to change a budget they already have. |
| `update_domain` | `manage_domain` | Update an existing domain's name or description. |
| `update_event` | `update_entity` | Update an existing calendar event. Find by title, then apply changes. |
| `update_expense` | `update_entity` | Update an existing expense. Find by description (partial match), then apply changes. |
| `update_goal` | `update_entity` | Update or complete a goal. Use when user wants to: mark a goal complete/done/achieved ('I finished my goal', 'mark goal as done'), change th |
| `update_habit` | `update_entity` | Update a habit. Find by name (partial match), then apply changes. Use this to reschedule a habit — e.g. 'move my lisinopril to the evening'  |
| `update_income` | `update_entity` | Update an EXISTING income entry (amount, source/description, frequency, category, date). Find it by its description/source (partial match).  |
| `update_journal` | `update_entity` | Update an existing journal entry's content, mood, or tags. |
| `update_liability` | `update_entity` | Update fields on an existing liability profile. Use for: balance corrections ('my mortgage balance is now $410k'), rate changes ('refinanced |
| `update_liability_payment` | `update_entity` | Edit a RECORDED payment on a liability/loan/bill (fix a typo'd amount, wrong date, add a note). Finds the liability by name, then targets th |
| `update_memory` | `update_entity` | Update the value of an EXISTING saved memory/fact. Find it by key or current content (partial match), then store the corrected value. Use fo |
| `update_obligation` | `update_entity` | Update or manage an EXISTING recurring bill/obligation and its calendar series (never create a new one). Find by name (partial match), then  |
| `update_profile` | `update_profile` | Update an existing profile's data. Use this for personal info (blood type, allergies, height, birthday, phone number), pet info (breed, weig |
| `update_reminder` | `update_entity` | Move or rename an EXISTING reminder ('move my dentist reminder to 4pm', 'push that reminder to tomorrow'). Finds the pending reminder by tit |
| `update_task` | `update_entity` | Update an existing task. Find by title (partial match), then apply changes like new title, description, priority, due date, or status. Rich  |
| `update_tracker` | `update_entity` | Update an existing tracker's name, category, or unit. |
| `update_tracker_entry` | `update_tracker_entry` | Update/edit a previously logged tracker entry. Use when user says 'change my X log to Y', 'I actually slept 8 hours not 7', 'update today's  |
| `upload_document` | `manage_document` | Help the user upload a document or file. This cannot be done programmatically — the user must use the attachment (paperclip) button in the c |

## Read-only tools (35)

| Tool | Action type | Description |
|---|---|---|
| `explain_dashboard_item` | `retrieve` | Explain WHY an item appears (or doesn't) on the dashboard — 'why is the mortgage in today's agenda?', 'why does this task show as upcoming?' |
| `find_duplicates` | `retrieve` | Find likely duplicate records — 'do I have duplicate expenses?', 'find duplicate profiles', 'any doubled-up tasks?'. Heuristic candidates ON |
| `find_orphans` | `retrieve` | Scan the user's data for orphaned / inconsistent ownership records — 'check my data for orphaned records', 'is my data healthy?', 'find brok |
| `generate_chart` | `retrieve` | Generate a REAL VISUAL CHART rendered directly in the chat. USE THIS whenever the user says 'show', 'chart', 'graph', 'visualize', 'plot', ' |
| `generate_report` | `retrieve` | Generate a comprehensive multi-section report with charts, tables, and metrics. Use for 'report', 'summary', 'scorecard', 'digest', 'overvie |
| `generate_table` | `retrieve` | Generate a formatted interactive data table in the chat. Use for 'show all', 'list', 'table of', or structured data requests. |
| `get_asset_rollup` | `retrieve` | Get the total value rollup for a profile and all its nested children/descendants. Use when the user asks 'what is the total value of my home |
| `get_budget_summary` | `retrieve` | Get the budget vs actual spending summary for a month. Shows all budget categories with budgeted amounts and actual spending. When the user  |
| `get_cashflow` | `retrieve` | Get weekly cash flow projections vs actuals for a given month. Shows projected and actual income/expenses by week. |
| `get_dose_history` | `retrieve` | Show the dose-by-dose medication log — 'show my Lisinopril history', 'when did I last take my meds?'. Read-only list of taken/skipped/missed |
| `get_entity_history` | `retrieve` | Show the change history / audit trail for a record — 'who changed the mortgage balance and when', 'what happened to my Honda asset', 'show t |
| `get_goal_progress` | `retrieve` | Check progress on goals. Use when user asks 'How am I doing on my goals?' or 'What's my goal progress?' or mentions a specific goal. |
| `get_liability_summary` | `retrieve` | Get a complete summary of one liability OR all liabilities: balances, payoff timeline, total interest paid + projected, recent payments, lin |
| `get_loan_schedule` | `retrieve` | Get the full amortization schedule for a loan, showing each payment with principal, interest, and remaining balance |
| `get_missed_doses` | `retrieve` | Report missed/skipped medication doses over a window — 'what doses did I miss this week?', 'how's my Metformin adherence?', 'did Max get all |
| `get_profile_data` | `retrieve` | Get ALL data for a specific person or pet profile — their tasks, expenses, trackers, events, documents, obligations, child profiles (assets/ |
| `get_related` | `retrieve` | Get all entities related/linked to a given entity. Use when user asks 'what's related to X', 'show everything for my Tesla', 'what expenses  |
| `get_relationships` | `retrieve` | Return the full relationship graph for a profile (1 or 2 hops): all linked assets, liabilities, and people with their roles and ownership %. |
| `get_summary` | `retrieve` | Get summary statistics for a specific entity type or all data. Use when the user asks for an overview, stats, totals, or 'how many'. Support |
| `navigate` | `retrieve` | Navigate the UI to a specific page. Use when user says 'go to...', 'show me...', 'open dashboard', etc. |
| `open_document` | `retrieve` | Search for and open a stored document. Returns document data for display. |
| `query_calendar` | `retrieve` | Query the unified calendar timeline for a date range. Returns events, tasks with due dates, habit schedules, and obligation due dates. Use w |
| `query_expenses` | `retrieve` | Query expenses with optional filters. Use when user asks 'last 5 expenses', 'how much did I spend on food this month?', 'show my recent purc |
| `query_net_worth_history` | `retrieve` | Answer 'why did my net worth change today' / 'how did Bob's net worth change'. Reads the daily net-worth snapshot history and compares today |
| `query_tasks` | `retrieve` | Query tasks with optional filters. Use when user asks 'tasks due this week', 'completed tasks', 'show all my active tasks', 'overdue tasks'. |
| `recall_actions` | `retrieve` | Recall recent actions — shows the last N things you did in Portol. Use when user asks 'what did I just do?', 'show recent actions', 'what ha |
| `refresh_ai_summary` | `retrieve` | Refresh the AI-generated summary for the user's dashboard or a specific profile. Returns a current financial snapshot and health data overvi |
| `refresh_dashboard` | `retrieve` | Force-refresh the dashboard — 'refresh my dashboard', 'my counts look stale', 'reload my data'. Clears the server's per-user caches, bumps t |
| `retrieve_document` | `retrieve` | Retrieve and display a document. Use when user asks to see, open, show, or view a document. Understands ownership — e.g., 'show my mom\'s bi |
| `search` | `retrieve` | Search across all entities (profiles, trackers, tasks, expenses, events, habits, obligations, documents, memories). Use when the user asks t |
| `search_documents` | `retrieve` | Search for documents linked to a specific profile. Use when the user asks 'show me all documents linked to my house', 'what documents does m |
| `set_dashboard_scope` | `retrieve` | Change the dashboard/Executive PROFILE FILTER — i.e. WHOSE data the dashboard shows. Use whenever the user says 'show me <Name>'s dashboard  |
| `spending_analytics` | `retrieve` | Compute spending analytics for a time period, grouped by category. Optionally compare with the previous period. Use when user asks 'spending |
| `validate_dashboard_counts` | `retrieve` | Check that dashboard tile counts match the database — 'are my dashboard numbers right?', 'the task count looks wrong'. Recomputes each tile  |
| `validate_profile_isolation` | `retrieve` | Verify every record's profile links point only at the user's own profiles — 'is my data isolated correctly?', 'check profile links', 'valida |
